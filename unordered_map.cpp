 unordered_map<int,vector<int>> map;
//Print Map
for (auto& it: map) {
	cout << it.first<<" ";
	for(auto it1:it.second)
	    cout<< it1<<" ";
	cout<<endl;
}
//Find an element
.clear() to clear the entries from the vector.